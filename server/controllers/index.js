const roles = require('./role');
const users = require('./user');
const documents = require('./document');
const authenticates = require('./authenticate');

module.exports = {
  roles,
  users,
  documents,
  authenticates,
};
